# Built binaries (anchored, so it does not also match the module's licence
# directory of the same name under testdata/example-module/LICENSES/go/...)
/build/
/testdata/example-module/oss-ensure-license-example

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store

# Claude Code local state
.claude/
